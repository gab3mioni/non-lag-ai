economic_mobilisation_cat = {

	GER_economic_mobilisation = {
	
		icon = generic_industry

		fire_only_once = no

		days_re_enable = 180

		allowed = {
			always = yes
		}

		available = {
			has_war = yes
			OR = {
				has_idea = war_economy
				has_idea = tot_economic_mobilisation
			}
		}

		visible = {
			tag = GER
			has_dynamic_modifier = { modifier = GER_blitzkrieg_modifier }
			NOT = { has_idea = GER_total_war_production_5 }
			has_global_flag = enclave_enabled
		}

		complete_effect = {
			GER_go_up_on_economic_mobilisation = yes
		}

		ai_will_do = {
			factor = 0
			modifier = {
				add = 200
				check_variable = { GER_blitzkrieg < 0 }
			}
			modifier = {
				add = 200
				check_variable = { GER_blitzkrieg < 20 }
				date < 1940.6.1 #if we dropped low enough due to Poland or WW1-style warfare, we need to do it
			}
			modifier = {
				add = 200
				check_variable = { GER_blitzkrieg < 40 }
				has_war_with = SOV #if we dropped low enough due to Soviet Union or prolonged WW1-style warfare, we need to do it
			}
			modifier = {
				add = 200
				AND = {
					OR = {
						has_equipment = { infantry_equipment < -5000 }
						has_equipment = { artillery_equipment < -2000 }
					}
					date < 1941.6.24
				}
			}
			modifier = {
				add = 200
				AND = {
					OR = {
						has_equipment = { infantry_equipment < -20000 }
						has_equipment = { artillery_equipment < -5000 }
					}
					date > 1941.6.24
				}
			}
		}
	}

	USA_economic_mobilisation = {
	
		icon = generic_industry

		fire_only_once = no

		days_re_enable = 240

		allowed = {
			always = yes
		}

		available = {
			has_war = yes
			OR = {
				has_idea = war_economy
				has_idea = tot_economic_mobilisation
			}
		}

		visible = {
			tag = USA
			NOT = { has_idea = USA_total_war_production_5 }
			has_global_flag = enclave_enabled
		}

		complete_effect = {
			USA_go_up_on_economic_mobilisation = yes
		}

		ai_will_do = {
			factor = 200
			modifier = {
				factor = 0
				has_idea = USA_total_war_production_4
				NOT = { has_global_flag = d_day_happened }
			}
		}
	}

	generic_economic_mobilisation_rich = {
	
		icon = generic_industry

		fire_only_once = no

		days_re_enable = 365

		allowed = {
			always = yes
		}

		available = {
			has_war = yes
			OR = {
				has_idea = war_economy
				has_idea = tot_economic_mobilisation
			}
		}

		visible = {
			NOT = { tag = USA }
			NOT = { tag = GER }
			NOT = { tag = SOV }
			NOT = { has_idea = GER_total_war_production_5 }
			has_global_flag = enclave_enabled
		}

		complete_effect = {
			custom_effect_tooltip = "This is only a placeholder effect. Countries and conditions specific malus will be made to tailor to your lack of steel and manpower"
			GER_go_up_on_economic_mobilisation = yes
		}

		ai_will_do = {
			factor = 0
			modifier = {
				add = 200
				is_in_faction_with = GER
				GER = { is_faction_leader = yes }
				GER = { check_variable = { GER_blitzkrieg < -25 } }
				NOT = { has_idea = GER_total_war_production_5 }
			}
			modifier = {
				add = 200
				is_in_faction_with = GER
				GER = { is_faction_leader = yes }
				GER = { check_variable = { GER_blitzkrieg < 10 } }
				date < 1940.6.1 #if we dropped low enough due to Poland or WW1-style warfare, we need to do it
				NOT = { has_idea = GER_total_war_production_4 }
			}
			modifier = {
				add = 200
				is_in_faction_with = GER
				GER = { is_faction_leader = yes }
				GER = { check_variable = { GER_blitzkrieg < 30 } }
				has_war_with = SOV #if we dropped low enough due to Soviet Union or prolonged WW1-style warfare, we need to do it
				NOT = { has_idea = GER_total_war_production_3 }
			}
			modifier = {
				add = 200
				OR = {
					tag = SAF
					tag = RAJ
				}
				AND = {
					date > 1942.8.4
				}
			}
			modifier = {
				add = 200
				AND = {
					OR = {
						has_equipment = { infantry_equipment < -2000 }
						has_equipment = { artillery_equipment < -300 }
					}
					is_major = no
				}
			}
			modifier = {
				add = 200
				AND = {
					OR = {
						has_equipment = { infantry_equipment < -10000 }
						has_equipment = { artillery_equipment < -3000 }
					}
					date < 1941.6.24
					NOT = { has_idea = GER_total_war_production_3 }
				}
			}
			modifier = {
				add = 200
				AND = {
					OR = {
						has_equipment = { infantry_equipment < -20000 }
						has_equipment = { artillery_equipment < -5000 }
					}
					date > 1941.6.24
					NOT = { has_idea = GER_total_war_production_4 }
				}
			}
			modifier = {
				add = 200
				surrender_progress > 0.3
				NOT = { has_idea = GER_total_war_production_5 }
			}
			modifier = {
				factor = 0
				has_idea = GER_total_war_production_2
				surrender_progress < 0.3
				NOT = { has_government = fascism }
				NOT = { tag = CHI }
			}
			modifier = {
				factor = 0
				has_idea = GER_total_war_production_4
				tag = CHI
			}
			modifier = {
				add = 200
				has_capitulated = yes
			}
		}
	}
}
	