setting_decision = {
	speed_optimization = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		visible = {
			is_ai = no
			NOT = { has_global_flag = speed_optimization_flag }
			is_historical_focus_on = yes
			date < 1936.2.1
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
		}

		complete_effect = {
			custom_effect_tooltip = "Will also limit the maximum collab subjects a country can make"
			hidden_effect = {
				set_global_flag = limited_number_of_collab
				set_global_flag = speed_optimization_flag
			}
			every_country = {
				limit = {
					OR = {
						tag = VIN
						tag = SYR
						tag = LEB
					}
					is_ai = yes
				}
				FRA = {
					annex_country = { target = PREV transfer_troops = no }
				}
			}
			every_country = {
				limit = {
					OR = {
						tag = KUW
						tag = EGY
						tag = PAL
						tag = JOR
					}
					is_ai = yes
				}
				ENG = {
					annex_country = { target = PREV transfer_troops = no }
				}
			}
			every_country = {
				limit = {
					OR = {
						tag = BRM
					}
					is_ai = yes
				}
				RAJ = {
					annex_country = { target = PREV transfer_troops = no }
				}
			}
			every_country = {
				limit = {
					OR = {
						tag = ELS
						tag = HON
						tag = GUA
						tag = NIC
						tag = COS
						tag = CUB
						tag = HAI
						tag = DOM
						tag = COL
						tag = ECU
						tag = PRU
						tag = PAN
						tag = BOL
						tag = VEN
					}
					is_ai = yes
				}
				GUA = {
					annex_country = { target = PREV transfer_troops = no }
					every_owned_state = { 
						add_core_of = GUA
					}
					add_ideas = dead_country
				}
			}
			every_country = {
				limit = {
					OR = {
						tag = SAU
						tag = YEM
					}
					is_ai = yes
				}
				SAU = {
					annex_country = { target = PREV transfer_troops = no }
					every_owned_state = { 
						add_core_of = SAU
					}
					add_ideas = dead_country
				}
			}
			every_country = {
				limit = {
					OR = {
						tag = OMA
					}
					is_ai = yes
				}
				ENG = {
					annex_country = { target = PREV transfer_troops = yes }
				}
			}
			every_country = {
				limit = {
					OR = {
						tag = TIB
						tag = BHU
					}
					is_ai = yes
				}
				TIB = {
					annex_country = { target = PREV transfer_troops = no }
					every_owned_state = { 
						add_core_of = TIB
					}
					add_ideas = dead_country
				}
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	limit_collab_number = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		visible = {
			is_ai = no
			NOT = { has_global_flag = limited_number_of_collab }
			NOT = { has_global_flag = speed_optimization_flag }
			date < 1936.2.1
			NOT = { has_global_flag = mp_difficulty }
		}

		complete_effect = {
			custom_effect_tooltip = "Limit the maximum collab an AI country can make"
			custom_effect_tooltip = "Will also prevent AI from creating too many new puppets on nonhistorical"
			set_global_flag = limited_number_of_collab
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	test_penal_battalion = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			NOT = { has_global_flag = mp_difficulty }
		}

		complete_effect = {
			set_technology = { LSM_penal_battalion = 1 }
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	test_SOV_vs_saadabad_pact = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			NOT = { has_global_flag = mp_difficulty }
		}

		complete_effect = {
			set_global_flag = saadabad_allowed
			custom_effect_tooltip = "Saadabad Pact will be formed when a country declares war on its signatories, before 50% world tension."
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	GER_test_FYP = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			GER = { is_ai = yes }
			NOT = { has_global_flag = mp_difficulty }
		}

		complete_effect = {
			custom_effect_tooltip = test_FYP_tt
			hidden_effect = {
				GER = { set_country_flag = FYP_preferred }
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	LSM_test_ban_trade_coordination = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
			NOT = { has_global_flag = banned_trade_coordination }
		}

		complete_effect = {
			set_global_flag = banned_trade_coordination
			every_country = {
				every_other_country = {
					remove_opinion_modifier = {
						target = PREV
						modifier = trade_relation_bonus_high
					}
				}
			}
			every_country = {
				every_other_country = {
					remove_opinion_modifier = {
						target = PREV
						modifier = trade_relation_bonus_high_1
					}
				}
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	LSM_test_ban_the_oil_trade = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
			NOT = { has_global_flag = LSM_oil_trade_banned }
		}

		complete_effect = {
			hidden_effect = {
				set_global_flag = LSM_oil_trade_banned
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	LSM_delete_allied_members = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
			NOT = { has_global_flag = LSM_remove_allied_welfare }
		}

		complete_effect = {
			hidden_effect = {
				set_global_flag = LSM_remove_allied_welfare
			}
			every_country = {
				limit = { is_subject_of = FRA }
				FRA = { annex_country = { target = PREV transfer_troops = no } }
			}
			RAJ = { annex_country = { target = BRM transfer_troops = no } }
			ENG = { annex_country = { target = JOR transfer_troops = no } }
			ENG = { annex_country = { target = PAL transfer_troops = no } }
			ENG = { annex_country = { target = EGY transfer_troops = no } }
			ENG = { 
				set_autonomy = { target = OMA autonomy_state = autonomy_free }
				remove_from_faction = OMA 
				transfer_state = 992
			}
			hidden_effect = {
				every_state = {
					limit = { 
						OR = {
							is_owned_by = FRA
							is_owned_by = ENG
						}
						has_resistance = yes
						compliance < 80
					}
					set_compliance = 80
				}
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	LSM_test_ban_the_ball_trade = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
			NOT = { has_global_flag = LSM_ball_trade_banned }
		}

		complete_effect = {
			hidden_effect = {
				set_global_flag = LSM_ball_trade_banned
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	LSM_test_ban_baltic_welfare_for_soviet_union = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
			NOT = { has_global_flag = SOV_welfare_disabled }
		}

		complete_effect = {
			hidden_effect = {
				set_global_flag = SOV_welfare_disabled
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	LSM_test_ban_swiss_gold = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
		}

		complete_effect = {
			hidden_effect = {
				set_global_flag = swiss_gold_banned
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	LSM_test_ban_swiss_trade = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
		}

		complete_effect = {
			hidden_effect = {
				set_global_flag = swiss_trade_banned
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	delete_indochina = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		visible = {
			is_ai = no
			date < 1936.2.1
			VIN = { is_ai = yes }
			VIN = { is_subject_of = FRA }
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
		}

		complete_effect = {
			FRA = { annex_country = { target = VIN transfer_troops = yes } }
			hidden_effect = {
				every_state = {
					limit = { 
						is_core_of = VIN
						is_owned_by = FRA
					}
					set_compliance = 80
				}
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	LSM_test_SOV_extra_research_slot = {
		available = { 
			is_ai = no 
			date < 1936.2.1
		}

		fire_only_once = yes

		visible = {
			is_ai = no
			date < 1936.2.1
			OR = {
				NOT = {
					any_other_country = { is_ai = no }
				}
				has_country_flag = host
			}
		}

		complete_effect = {
			SOV = {
				add_research_slot = 1
			}
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}

	sino_japanese_war_balance_sp = {
		available = { 
			is_ai = no 
			CHI = { has_war = no }
		}

		visible = {
			is_ai = no
			CHI = { is_ai = yes }
			is_historical_focus_on = yes
			NOT = {
				any_other_country = { is_ai = no }
			}
			date < 1937.1.1
		}

		complete_effect = {
			country_event = LSM_equipment.31
		}

		cost = 0
		ai_will_do = {
			factor = 0
		}
		
	}
}