###########################
###########################

add_namespace = LSM_generic

country_event = {
	id = LSM_generic.1
	
	trigger = {
		tag = FRA
		has_war_with = GER
		is_historical_focus_on = yes
	}

	fire_only_once = yes
	
	mean_time_to_happen = { days = 1 }

	hidden = yes

	immediate = {
		set_global_flag = france_is_at_war
	}
}
